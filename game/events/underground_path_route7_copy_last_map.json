{
 "$comment": "scripts/UndergroundPathRoute7Copy.asm — sets wLastMap = ROUTE_7 on load, so this connector's LAST_MAP exits go back to Route 7 regardless of which side you entered from (gh #114).",
 "id": "event:underground_path_route7_copy_last_map",
 "trigger": {
  "kind": "enter",
  "map": "map:UndergroundPathRoute7Copy"
 },
 "commands": [
  {
   "cmd": "set_last_map",
   "map": "map:Route7"
  }
 ]
}
