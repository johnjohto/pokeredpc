{
 "$comment": "scripts/UndergroundPathRoute5.asm — sets wLastMap = ROUTE_5 on load, so this connector's LAST_MAP exits go back to Route 5 regardless of which side you entered from (gh #114).",
 "id": "event:underground_path_route5_last_map",
 "trigger": {
  "kind": "enter",
  "map": "map:UndergroundPathRoute5"
 },
 "commands": [
  {
   "cmd": "set_last_map",
   "map": "map:Route5"
  }
 ]
}
